spring:
  application:
    name: CalculadoradeJuros

  datasource:
    url: jdbc:postgresql://localhost:5432/calculadora
    username: postgres
    password: 8745

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
